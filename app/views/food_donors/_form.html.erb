<%= form_for(@food_donor) do |f| %>
  <% if @food_donor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_donor.errors.count, "error") %> prohibited this food_donor from being saved:</h2>

      <ul>
      <% @food_donor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="row">
  <div class="col-sm-4">
    <div class="input-group">
      <%= f.label :name %><br>
     <%= f.text_field :name %>
    </div>
    <div class="field">
    <%= f.label :email %><br>
    <%= f.email_field :email %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address, :class => 'add_location' %>
  </div>

  
  </div>
  <div class="col-sm-4">
  
    
      <div class="field">
        <%= f.label :available_till %><br>
        <%= f.text_field :available_till %>
    </div>
    <div class="field">
      <%= f.label :sufficient_for %><br>
      <%= f.text_field :sufficient_for %>
    </div>
    
    
    <div class="field">
        <%= f.label :food_details %><br>
        <%= f.text_field :food_details %>
    </div>

    <div class="field">
      <%= f.check_box :has_transport %>
      <%= f.label :has_transport %>
    </div>
  </div>
</div>

<%= f.hidden_field :lat, :id => 'region_lat' %>
<%= f.hidden_field :lang, :id => 'region_long' %>

<div class="row">
<div class="actions">
      <%= f.submit %>
    </div>
    </div>

  
  
  
  
<% end %>
