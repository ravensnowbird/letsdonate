<div class='container'>
  <h1>Dashboard</h1>
  <div class='col-lg-6'>
    <h3>Add regions in which you are available </h3>

    <%#= link_to "Add Regions", new_transporter_region_path(@transporter), class: "btn-default btn"%>

    <% if @transporter.present? %>
      <%= form_tag(transporter_create_regions_path(@transporter), {:method => :post}) do %>
      <div>
      </div>
      <%= add_region_fields(render "transporters/region_fields") %>
      <%= submit_tag "Submit",class: 'btn btn-success btn-sm' %>
      <% end %>
    <% elsif @ngo.present? %>
      <%= form_tag(ngo_create_regions_path(@ngo), {:method => :post}) do %>
      <div>
      </div>
      <%= add_region_fields(render "ngos/region_fields") %>
      <%= submit_tag "Submit",class: 'btn btn-success btn-sm' %>
      <% end %>
    <% end %>
  </div>
  <div class='col-lg-6'>
    <h2>Your regions</h2>
    <%# @transporter.suman %>
    <% if @added_regions.present? %>
    <div id="regions_table">
      <table class="table">
        <thead>
          <tr>
            <th>Address</th>
            <th>Latitude</th>
            <th>Longitude</th>
            <th></th>
          </tr>
        </thead>

        <tbody>
          <% @added_regions.each do |region| %>
          <tr id=<%= 'region_' + region["id"] %>>
            <td><%= region["address"] %></td>
            <td><%= region['lat'] %></td>
            <td><%= region['long'] %></td>
            <% if @transporter.present? %>
              <td><%= link_to "Delete",transporter_region_path(@transporter, region["id"]), method: 'delete', class: 'btn btn-xs btn-danger', remote: 'true' %></td>
            <% elsif @ngo.present? %>
              <td><%= link_to "Delete",ngo_region_path(@ngo, region["id"]), method: 'delete', class: 'btn btn-xs btn-danger', remote: 'true' %></td>
            <% end %>

          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <% else %>
    <h3>No regions added yet. </h3>
    <% end %>
  </div>
</div>
